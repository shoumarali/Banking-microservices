server:
  port: 8080

spring:
  application:
    name: "account-service"
  profiles:
    active: "default"

  datasource:
    url: jdbc:mysql://localhost:3306/accountsdb
    username: "root"
    password: "root"
  jpa:
    show-sql: true
  sql:
    init:
      mode: always

#  config:
#    import: "configserver:http://localhost:8071"

  rabbitmq:
    host: "localhost"
    port: 5672
    username: "guest"
    password: "guest"

management:
  endpoints:
    web:
      exposure:
        include: "*"